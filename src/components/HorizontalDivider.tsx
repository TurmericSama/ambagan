import { FC } from "react";
import { Divider } from "@mui/material";

const HorizontalDivider: FC = () => {
  return <Divider orientation="horizontal" sx={{ my: 2 }} />;
};

export default HorizontalDivider;
